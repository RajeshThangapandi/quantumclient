export interface User {
    name: string;            // User's full name
    dateOfBirth: string;     // User's date of birth in ISO 8601 format
    email: string;           // User's email address
    password: string;        // User's password
  }